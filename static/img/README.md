# Images to add

Drop these files here (referenced by config + home page):

- `profile.jpg` — headshot, ~800×800px square
- `logo.png` — small site logo/mark (transparent PNG)
- `favicon.ico` — browser tab icon

Until you add `profile.jpg`, the homepage hero image will be missing — the rest of the site renders fine.

# Hugo build output
/public/
/resources/_gen/
.hugo_build.lock

# OS / editor cruft
.DS_Store
Thumbs.db

# Node (only if you add tooling later)
node_modules/

# Claude local cache
.claude/

# Do NOT commit secrets or backups
*.env
wp-backup/
# contains your server connection details — keep private
_docs/DEPLOY.md
# raw source images (processed copies live in static/img)
_design/
# temp build files
.hugo_build*.lock

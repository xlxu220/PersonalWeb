# Hugo build output
/public/
/resources/_gen/
.hugo_build.lock

# OS / editor cruft
.DS_Store
Thumbs.db

# Node (only if you add tooling later)
node_modules/

# Do NOT commit secrets or backups
*.env
wp-backup/
_docs/DEPLOY.md    # contains your server connection details — keep private
_design/           # raw source images (processed copies live in static/img)
.hugo_build*.lock  # temp build files
